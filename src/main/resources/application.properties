# Global configuration for sending a single message/image to all contacts
# If set, these will override individual messages in the CSV
# whatsapp.global.message=This is a broadcast message!
# whatsapp.global.image.path=src/main/resources/sample.jpg

# Interval between messages in milliseconds (default is 10000ms = 10s)
whatsapp.interval.ms=10000
